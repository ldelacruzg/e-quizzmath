class DateRangeLeaderboard {
  final DateTime startDate;
  final DateTime endDate;
  final DateTime currentDate;

  DateRangeLeaderboard({
    required this.startDate,
    required this.endDate,
    required this.currentDate,
  });
}
